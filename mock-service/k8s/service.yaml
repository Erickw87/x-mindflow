apiVersion: v1
kind: Service
metadata:
  name: mock-user-service-v1-1
  labels:
    app: mock-user-service
    version: v1.1.0
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mock-user-service
    version: v1.1.0
---
apiVersion: v1
kind: Service
metadata:
  name: mock-user-service-v1-2
  labels:
    app: mock-user-service
    version: v1.2.0
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mock-user-service
    version: v1.2.0
---
apiVersion: v1
kind: Service
metadata:
  name: mock-user-service
  labels:
    app: mock-user-service
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: mock-user-service
